# Cloudflare 固定网址

当前固定网址：

```text
https://menu.qingyeyue.xyz
```

已使用的 Tunnel：

```text
menu-system
```

每天使用时双击：

```text
start-all-fixed-tunnel.cmd
```

它会打开：

- `Menu System`
- `Cloudflare Fixed Tunnel`

两个窗口都要保持打开。

本地固定隧道配置文件：

```text
cloudflared-fixed.yml
```

这个文件只保存在本机，不上传 GitHub。
